import 'package:flutter/material.dart';

class SpecifyDialog extends StatelessWidget {
  const SpecifyDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
